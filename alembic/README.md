⚠️ Note: When creating a new table, add the `if_not_exists=True` clause.

- Create a new migration script with an autogenerated message:

```bash
alembic revision --autogenerate -m "create tables"                                
```

- Create a new migration script with a custom message:

```bash
 alembic revision -m "add emails"                  
```

- Apply the latest migrations to the database:

```bash
alembic upgrade head
```

- Downgrade the database to the previous migration:

```bash
alembic downgrade -1
```


